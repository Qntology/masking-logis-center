# 개발 기획서: AI 커머스 에이전트 (Branching & Pruning 아키텍처)

## 1. 개요
본 프로젝트는 Gemini 3.1 Flash-Lite의 Context Caching 기능을 최적화하여, 커머스 데이터의 무결성을 유지하면서 비용과 속도를 극대화하는 것을 목적으로 함.

## 2. 핵심 아키텍처: Trunk & Branch 전략
- **Main Trunk (메인 채널):** 전체 주문 상태(YAML SSOT)를 캐싱 유지. 판매자 조회 및 대화의 메인 환경.
- **Sub-Branch (서브 채널):** 변경(취소, 반품, 정보 수정) 이벤트 발생 시 생성. 독립된 스레드에서 작업 후 결과만 동기화하고 즉시 폐기(Pruning).

## 3. 기술적 구현 원칙
### 3.1. 캐싱 전략 (Immutable Base Cache)
- 메인 채널의 캐시는 절대 수정(Overwrite)하지 않음. 
- 오직 이벤트가 발생할 때만 서브 채널을 통해 결과값(Diff)을 메인 장부(YAML)에 병합하고, 시스템 스케줄러를 통해 메인 채널의 캐시를 주기적으로 리빌드(Rebuild)함.

### 3.2. 상태 관리 (State-based Overwrite)
- 과거 로그(Log)를 무한 누적하지 않고, 오직 '최신 상태(Current State)'를 담은 YAML 파일만을 SSOT로 정의.

### 3.3. 대화 맥락 유지 (Bridge Handoff)
- 서브 채널 종료 시, 마지막 작업 결과를 요약(Summary)하여 메인 채널의 시스템 프롬프트/히스토리에 주입하여 맥락 단절 방지.

## 4. 커머스 이벤트 관리 프로세스 (예시: 부분 취소)
1. **이벤트 탐지:** 사용자 요청 (액션) 발생.
2. **브랜치 생성:** 독립된 서브 채널(Sub-Branch) 생성.
3. **데이터 추출:** 변경 대상 데이터만 파싱(JSON).
4. **결과 동기화:** 시스템 DB(LanceDB/파일) 업데이트.
5. **가지치기 (Pruning):** 서브 채널 삭제 및 요약본 메인 채널 전송.

## 5. 단계별 추진 전략
- **Phase 1:** 고정된 상태 기반의 YAML 장부 아키텍처 정의 및 메인 채널 캐싱 구현.
- **Phase 2:** 하네스(Browser-use)를 통한 데이터 정규화 및 브랜치 작업 로직 구축.
- **Phase 3:** 이벤트 발생 시 서브 채널 동적 생성 및 맥락 연결(Bridge) 시스템 완성.

---
본 문서는 커머스 데이터의 무결성과 컨텍스트 효율을 동시에 달성하기 위한 기본 기획안임.
