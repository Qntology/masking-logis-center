[PRD] AI 커머스 자동화 에이전트 개발 기획서

## 1. 개요
본 프로젝트는 커머스 데이터를 API 연동 및 브라우저 오토메이션으로 관리하는 AI 에이전트를 구축하는 것입니다. 가장 큰 난관인 '컨텍스트 오염', '비용 폭발', '동시성 충돌'을 해결하기 위해 'Main Trunk & Sub-Branch' 구조와 'Task Queue' 아키텍처를 채택합니다.

## 2. 아키텍처 원칙 (Core Principles)
- **State-Based Overwrite:** 로그 누적 방지, 최종 상태 중심의 데이터 갱신.
- **Trunk & Branch (Branching & Pruning):** 
    - **Main Trunk:** 일상 조회 및 맥락 유지(읽기 전용).
    - **Sub-Branch:** 이벤트(취소, 반품 등) 발생 시 임시 채널 개설, 작업 후 폐기(가지치기).
- **Task Queue (Orchestration):** 동시 다발적 요청(주문 취소 등)의 순차적 처리를 위한 비동기 작업 큐 도입.
- **Zero-Config Parser (Harness):** HTML에서 메타데이터를 제거하고 핵심 정보만 표준화된 YAML/CSV로 추출하는 백엔드 처리.

## 3. 데이터 구조 (Schema)
YAML 기반의 계층형 장부 구조를 사용하여 상태 변화를 효율적으로 기록합니다.

```yaml
주문번호:
  고객정보: ...
  최종상태: ...
  상품목록:
    - 품명: ...
      상태: ...
      송장: ...
```

## 4. 실무 시나리오: 이벤트 처리 파이프라인
1. **이벤트 탐지:** 하네스가 주문 페이지 내 변화를 감지.
2. **큐 적재 (Task Queue):** 동시 요청을 큐에 적재하여 Race Condition 방지.
3. **라우팅:** 단순 조회는 Main에서, 수정은 Sub로 분기.
4. **처리:** Sub-Branch에서 상태 업데이트 수행.
5. **합치기(Merge):** Sub-Branch의 결과를 최종 YAML에 업데이트.
6. **가지치기(Pruning):** Sub-Branch 대화창 삭제 및 상태 캐시 최신화.

## 5. 단계별 기획안
- **Phase 1 (기초):** 도메인별 표준 장부 포맷(YAML) 정의 및 입/출력 인터페이스 구현.
- **Phase 2 (하네스):** 웹페이지(HTML)에서 표준 포맷으로 변환하는 평탄화 스크립트 작성 (타깃: 스마트스토어/쿠팡).
- **Phase 3 (브랜칭 및 큐):** 비동기 작업 큐 및 채널 라우팅, 상태 업데이트 로직 구현.
- **Phase 4 (검증):** 이벤트 처리 후 전체 캐시 재빌드(Re-caching) 최적화.
